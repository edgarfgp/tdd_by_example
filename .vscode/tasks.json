{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/tdd_by_example.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}